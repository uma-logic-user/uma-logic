name: 📚 過去データ一括取得

on:
  workflow_dispatch:
    inputs:
      years:
        description: '取得する年（スペース区切り、例: 2024 2025）'
        required: false
        default: ''

jobs:
  fetch-historical:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6時間タイムアウト
    
    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🐍 Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 ディレクトリを作成
        run: |
          mkdir -p data
          mkdir -p data/archive

      - name: 📚 過去データを取得
        run: |
          if [ -n "${{ github.event.inputs.years }}" ]; then
            python scripts/fetch_historical_data.py ${{ github.event.inputs.years }}
          else
            python scripts/fetch_historical_data.py
          fi

      - name: 💾 変更をコミット＆プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git pull --rebase origin main || git pull origin main
          git add data/
          if git diff --staged --quiet; then
            echo "📭 変更なし"
          else
            git commit -m "📚 過去データ取得: $(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M')"
            git push
          fi
