name: 📚 過去データ一括取得

on:
  workflow_dispatch:
    inputs:
      years:
        description: '取得する年（スペース区切り、例: 2024 2025）'
        required: false
        default: ''

env:
  TZ: Asia/Tokyo

jobs:
  fetch-historical:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6時間

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: pip install -r requirements.txt

      - name: 📁 ディレクトリ作成
        run: |
          mkdir -p data/archive
          mkdir -p data/models

      - name: 📚 過去データを取得
        run: |
          if [ -n "${{ github.event.inputs.years }}" ]; then
            python scripts/fetch_historical_data.py ${{ github.event.inputs.years }} || echo "Historical fetch skipped"
          else
            python scripts/fetch_historical_data.py || echo "Historical fetch skipped"
          fi

      - name: 💾 変更をコミット＆プッシュ（コンフリクト回避）
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action Bot"
          
          # 変更をステージング
          git add data/ || true
          
          # 変更がある場合のみコミット
          if ! git diff --staged --quiet; then
            # 一時的にstashして最新を取得
            git stash push -m "temp-changes" || true
            
            # 最新を取得（コンフリクト回避）
            git fetch origin main
            git reset --hard origin/main
            
            # stashを戻す
            git stash pop || git checkout stash -- . || true
            
            # 再度ステージングしてコミット
            git add data/ || true
            git commit -m "📚 過去データ取得: $(date +'%Y-%m-%d %H:%M')" || true
            git push origin main || true
          else
            echo "📭 変更なし"
          fi
