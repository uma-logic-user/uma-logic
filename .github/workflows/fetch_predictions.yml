name: 🐎 予想データ取得

on:
  schedule:
    - cron: '0 22 * * 5'
    - cron: '0 22 * * 6'
  workflow_dispatch:

jobs:
  fetch-predictions:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🐍 Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 データディレクトリを作成
        run: mkdir -p data

      - name: 🏇 レースデータを取得
        run: python scripts/fetch_race_data.py

      - name: 🧮 高精度スコア計算
        run: python scripts/calculator_pro.py

      - name: 💾 変更をコミット＆プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git pull --rebase origin main || git pull origin main
          git add data/
          if git diff --staged --quiet; then
            echo "📭 変更なし"
          else
            git commit -m "🐎 予想更新: $(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M')"
            git push
          fi
