name: 📊 レース結果取得

on:
  schedule:
    # 土曜18:00 JST (UTC 09:00)
    - cron: '0 9 * * 6'
    # 日曜18:00 JST (UTC 09:00)
    - cron: '0 9 * * 0'
  workflow_dispatch:
    inputs:
      target_date:
        description: '対象日 (YYYYMMDD形式、空欄で自動判定)'
        required: false
        default: ''

jobs:
  fetch-results:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 データディレクトリを作成
        run: mkdir -p data

      - name: 🏁 レース結果を取得
        run: |
          echo "=========================================="
          echo "🏁 レース結果取得開始"
          echo "=========================================="
          if [ -n "${{ github.event.inputs.target_date }}" ]; then
            python scripts/update_results.py ${{ github.event.inputs.target_date }}
          else
            python scripts/update_results.py
          fi

      - name: 💾 変更をコミット＆プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add data/
          if git diff --staged --quiet; then
            echo "📭 変更なし、コミットをスキップ"
          else
            git commit -m "📊 結果更新: $(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ プッシュ完了"
          fi
