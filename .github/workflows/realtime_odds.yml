name: 💹 リアルタイムオッズ取得

on:
  workflow_dispatch:
    inputs:
      race_id:
        description: 'レースID (例: 202506010101)'
        required: false
        default: ''

jobs:
  fetch-odds:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 データディレクトリを作成
        run: mkdir -p data

      - name: 💹 リアルタイムオッズを取得
        run: |
          echo "=========================================="
          echo "💹 リアルタイムオッズ取得"
          echo "=========================================="
          python scripts/scraper_realtime.py

      - name: 💾 変更をコミット＆プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add data/
          if git diff --staged --quiet; then
            echo "📭 変更なし、コミットをスキップ"
          else
            git commit -m "💹 オッズ更新: $(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ プッシュ完了"
          fi
