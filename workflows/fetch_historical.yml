# .github/workflows/fetch_historical.yml
# UMA-Logic PRO - 過去データ一括取得ワークフロー
# 初回セットアップ時に使用

name: 📚 過去データ一括取得

on:
  workflow_dispatch:
    inputs:
      years:
        description: '取得する年（スペース区切り、例: 2024 2025）'
        required: false
        default: ''
      months:
        description: '取得する月（1-12、空欄で全月）'
        required: false
        default: ''
      notify:
        description: '通知を送信する'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  TZ: Asia/Tokyo

jobs:
  fetch-historical:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6時間

    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🐍 Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 ディレクトリを作成
        run: |
          mkdir -p data/archive
          mkdir -p data/models

      - name: 📚 過去データを取得
        id: historical
        run: |
          echo "=========================================="
          echo "📚 過去データ一括取得"
          echo "=========================================="
          
          ARGS=""
          if [ -n "${{ github.event.inputs.years }}" ]; then
            ARGS="$ARGS ${{ github.event.inputs.years }}"
          fi
          
          python scripts/fetch_historical_data.py $ARGS
          
          echo "historical_status=success" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: 📂 アーカイブを整理
        run: |
          python scripts/archive_manager.py --archive-all
          python scripts/archive_manager.py --rebuild-index
        continue-on-error: true

      - name: 💾 変更をコミット＆プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "UMA-Logic Bot"
          git pull --rebase origin main || git pull origin main
          git add data/
          if git diff --staged --quiet; then
            echo "📭 変更なし"
          else
            git commit -m "📚 過去データ取得: $(date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ プッシュ完了"
          fi

      - name: 📱 通知を送信
        if: github.event.inputs.notify != 'false'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          LINE_NOTIFY_TOKEN: ${{ secrets.LINE_NOTIFY_TOKEN }}
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        run: |
          python scripts/notifier.py --type historical --status ${{ steps.historical.outputs.historical_status || 'completed' }}
        continue-on-error: true
