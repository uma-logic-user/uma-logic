# .github/workflows/fetch_results.yml
# UMA-Logic PRO - レース結果取得ワークフロー
# スマホ・PC両対応（手動実行可能）

name: 📊 レース結果取得

on:
  schedule:
    # 土曜18:00 JST (UTC 09:00)
    - cron: '0 9 * * 6'
    # 日曜18:00 JST (UTC 09:00)
    - cron: '0 9 * * 0'
  workflow_dispatch:
    inputs:
      target_date:
        description: '対象日 (YYYYMMDD形式、空欄で本日)'
        required: false
        default: ''
      notify:
        description: '通知を送信する'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  TZ: Asia/Tokyo

jobs:
  fetch-results:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🐍 Python環境をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 依存パッケージをインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📁 ディレクトリを作成
        run: |
          mkdir -p data/archive
          mkdir -p data/models

      - name: 📊 レース結果を取得
        id: results
        run: |
          echo "=========================================="
          echo "📊 レース結果取得開始"
          echo "=========================================="
          
          if [ -n "${{ github.event.inputs.target_date }}" ]; then
            python scripts/update_results.py ${{ github.event.inputs.target_date }}
          else
            python scripts/update_results.py
          fi
          
          echo "results_status=success" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: 🎯 的中判定を実行
        run: |
          echo "=========================================="
          echo "🎯 的中判定"
          echo "=========================================="
          python scripts/calculator_pro.py --check-hits
        continue-on-error: true

      - name: 📚 アーカイブに保存
        run: |
          python scripts/archive_manager.py --archive-today
        continue-on-error: true

      - name: 💾 変更をコミット＆プッシュ
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "UMA-Logic Bot"
          git pull --rebase origin main || git pull origin main
          git add data/
          if git diff --staged --quiet; then
            echo "📭 変更なし"
          else
            git commit -m "📊 結果取得: $(date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ プッシュ完了"
          fi

      - name: 📱 通知を送信
        if: github.event.inputs.notify != 'false'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          LINE_NOTIFY_TOKEN: ${{ secrets.LINE_NOTIFY_TOKEN }}
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        run: |
          python scripts/notifier.py --type results --status ${{ steps.results.outputs.results_status || 'completed' }}
        continue-on-error: true
